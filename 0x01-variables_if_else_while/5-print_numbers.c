#include <stdio.h>
/**
 * main - this is the main function serving as the entry point for execution
 * Descripttion - how to print single digit numbers of base 10
 * Return: mostly 0
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}

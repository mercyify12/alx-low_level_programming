#include <stdio.h>
/**
 * main - this function serves as the starting point for program execution
 *
 * Return: most times it is 0
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
